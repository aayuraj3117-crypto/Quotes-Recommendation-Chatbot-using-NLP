version: "3.1"

intents:
  - greet
  - motivation

responses:
  utter_greet:
    - text: "Hey, How are you doing?"

  utter_motivation:
    - text: "Push yourself harder because no one else will do it for you."

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
